# Normalize to LF in the repo; Git converts to CRLF in the Windows working tree.
* text=auto eol=lf

# Binaries: never touch.
*.png binary
*.jpg binary
*.ico binary
*.bin binary
*.gguf binary
*.ggml binary
